<div class="d-flex justify-content-between align-items-center shadow position-fixed bottom-0 end-0 start-0 shadow-lg p-3 ">
    <h5>Total items in your cart : <%= @items.length %> </h5>
    <%= button_to 'Place Order and Pay', orders_path(@cart), method: :post, class: "btn btn-warning" %>
</div>